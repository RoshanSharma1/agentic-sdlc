# Project Rules

- Follow modular architecture — each module has a single responsibility
- Write unit tests for all features before considering a phase done
- Use descriptive commit messages in the format `type(scope): description`
- Always update docs after implementing a feature
- No secrets or credentials in code — use environment variables
- Prefer composition over inheritance
- Keep functions small and focused (< 50 lines)
