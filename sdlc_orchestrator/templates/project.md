# Project: {{PROJECT_NAME}}

## Stack
{{STACK}}

## Architecture
(describe high-level component structure here)

## Key Domain Concepts
(list core entities and terminology)

## Folder Structure
(describe what each top-level directory owns)

## Conventions
- Naming: (e.g. snake_case for Python, camelCase for JS)
- File structure: (e.g. one class per file)
- Error handling: (e.g. raise exceptions, don't return error codes)

## Known Constraints
(e.g. must run in Docker, no external API calls in tests, etc.)

## Deployment
(e.g. Docker Compose locally, ECS in production)
